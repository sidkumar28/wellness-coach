<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wellnesscoach" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WellnessCoach" >
        <activity
            android:name=".Customer_report"
            android:exported="false" />
        <activity
            android:name=".Customer_Report"
            android:exported="false" /> <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- inputpage Activity -->
        <activity
            android:name=".inputpage"
            android:exported="false" /> <!-- Customer_value Activity -->
        <activity
            android:name=".Customer_value"
            android:exported="true"
            android:parentActivityName=".inputpage" />
    </application>

</manifest>